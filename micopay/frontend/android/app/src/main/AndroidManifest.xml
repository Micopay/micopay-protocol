<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
    android:allowBackup="false"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:networkSecurityConfig="@xml/network_security_config"
    android:usesCleartextTraffic="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Android App Links — intercepts https://app.micopay.xyz/claim/* at the OS level.
                android:autoVerify="true" triggers Digital Asset Links verification against
                https://app.micopay.xyz/.well-known/assetlinks.json at install time.
                Once verified, tapping a claim URL opens MicoPay directly without a browser
                disambiguation dialog. launchMode="singleTask" (already set above) ensures
                a backgrounded instance receives the URL via onNewIntent instead of spawning
                a second activity.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="app.micopay.xyz"
                    android:pathPrefix="/claim/" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
        Solo ubicación aproximada (~1-3 km), suficiente para el mapa de agentes
        cercanos. ACCESS_FINE_LOCATION se retiró en la auditoría 2026-08
        (ISSUE-08): todas las llamadas de geolocalización pasan
        enableHighAccuracy:false, así que el permiso preciso no se usaba. Pedirlo
        obliga además al diálogo de "ubicación precisa" de Android 12+, que
        tiene peor tasa de aceptación, y exige justificación en Play Console.

        POST_NOTIFICATIONS y la meta-data de Firebase Cloud Messaging también se
        retiraron: no hay ningún plugin de push instalado, no existe
        google-services.json, y el escaneo de cadenas del classes.dex del APK
        compilado da 0 coincidencias para "messaging" y "PushNotification".
        Ambos vuelven solos el día que se instale el plugin de notificaciones.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
